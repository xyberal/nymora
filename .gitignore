# Build output
/target/
/measure/target/
/nymora-plonk/target/

# This is a library workspace, so the lockfile is not committed.
# The measurement harness commits its lockfile: pinned dependencies are part of
# reproducing the numbers its README records.
/Cargo.lock

# Local environment
.env
.env.*

# macOS
.DS_Store
